---
title: AI Review Commons
description: 面向人工智能论文审稿的双语提示词库。
template: splash
hero:
  title: AI Review Commons
  tagline: 面向人工智能论文审稿的双语提示词库。先选身份和任务，再复制提示词。
  actions:
    - text: 直接选择提示词
      link: /ReviewPrompt/prompts/
      icon: right-arrow
    - text: 提交提示词
      link: /ReviewPrompt/contribute/
      variant: secondary
---

import ResourceGrid from '../../components/ResourceGrid.astro';
import TaskRecommender from '../../components/TaskRecommender.astro';
import { catalog, getFeaturedResources } from '../../lib/catalog';

export const featured = getFeaturedResources(catalog);

<TaskRecommender />

<ResourceGrid
  title="当前只保留两个提示词"
  description="完整论文审稿和 Rebuttal 是第一版核心任务。"
  resources={featured.prompts}
/>

## 使用原则

- 只保留作者和审稿人两种使用模式。
- 只覆盖完整论文和 Rebuttal 两个任务。
- 领域固定为人工智能。
- AI 输出只作为辅助，不替代人类判断。
