---
title: 审一篇论文
description: 当你正在审稿时，从这里开始。
---

import ResourceGrid from '../../../components/ResourceGrid.astro';
import TaskRecommender from '../../../components/TaskRecommender.astro';
import { catalog } from '../../../lib/catalog';

<TaskRecommender />

## 推荐路径

1. 打开完整论文审稿流程。
2. 复制完整结构化论文审稿 Prompt，或安装对应 Skill。
3. 提供论文正文、摘要、方法、实验，以及可用的会议 rubric。
4. 在定稿前再运行一次评分校准。

<ResourceGrid
  title="审稿人资源"
  resources={catalog.workflows.filter((item) => item.slug === 'full-paper-review').concat(catalog.prompts.filter((item) => item.slug === 'full-structured-peer-review'))}
/>
